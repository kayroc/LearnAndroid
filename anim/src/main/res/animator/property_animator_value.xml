<?xml version="1.0" encoding="utf-8"?><!--
    android:duration        动画持续时间（ms），必须设置，动画才有效果
    android:fillAfter       动画播放完后，视图是否会停留在动画开始的状态，默认为true
    android:fillBefore      动画播放完后，视图是否会停留在动画结束的状态，优先于fillBefore值，默认为false
    android:fillEnabled     是否应用fillBefore值，对fillAfter值无影响，默认为true
    android:interpolator    插值器，即影响动画的播放速度
    android:repeatCount     重复次数(动画的播放次数=重放次数+1) infinite:无限重复
    android:repeatMode      重复模式 restart代表正序重放，reverse代表倒序回放，默认为restart
    android:startOffset     动画延迟开始时间（ms）
    android:valueFrom       动画初始值
    android:valueTo         动画结束值
    android:valueType       变化值类型 floatType intType pathType colorType
-->
<animator xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="1000"
    android:valueType="floatType"
    android:valueFrom="0f"
    android:valueTo="100f"
    android:repeatCount="1"
    android:repeatMode="reverse"
    android:interpolator="@android:interpolator/linear"
    android:startOffset="100"/>